//
// Created by Jinx on 2022/10/14.
//

#include <stdio.h>

int main (){

int low, high, key;

    while (low <= high){
        int mid = ((low + high) / 2);
        if (key <)
    };
    do {

    } while ();
    while (scanf("%d", &numbers[++len]) != EOF);

}